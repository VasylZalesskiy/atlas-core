import {Ambulance,ExternalLink,Eye} from "lucide-react";

export default function BestActionCard({solution,result,t,onSearch,onView}){const medical=solution.mode==="emergency"||solution.goal?.scenario==="medical-emergency";if(medical)return <section className="bestActionCard emergency medicalAction"><span className="eyebrow">{t.bestActionNow}</span><h2>{t.medicalFlow.title}</h2><p className="medicalSafety">{t.medicalFlow.safety}</p><div className="actionButtons"><a className="emergencyButton" href="tel:103"><Ambulance/>{t.call103}</a><button className="primary" type="button" onClick={onSearch}><ExternalLink/>{t.medicalFlow.findHospital}</button></div><p className="externalSafety"><ExternalLink/>{t.externalSafety}</p></section>;
if(!result)return null;return <section className={`bestActionCard ${solution.mode}`}><span className="eyebrow">{t.bestActionNow}</span><h2>{result.title}</h2>{result.description&&<p>{result.description}</p>}<div className="actionButtons"><button className="primary" type="button" onClick={()=>onView(result)}><Eye/>{t.viewOpportunity}</button></div></section>}
