import {useState} from "react";
import supabase from "../services/supabase";

export default function Profile({t}){const [saved,setSaved]=useState(false);const [error,setError]=useState("");const [f,setF]=useState({name:"",city:"",can_help:"",have:"",can_share:"",useful:"",needs:"",contact:""});const fields=[["name",t.name],["city",t.city],["can_help",t.can],["have",t.have],["can_share",t.share],["useful",t.useful],["needs",t.need],["contact",t.contact]];async function submit(e){e.preventDefault();setError("");if(supabase){const payload={slug:`${f.name.toLowerCase().replace(/[^a-zа-яіїє0-9]+/gi,"-")}-${Date.now().toString().slice(-5)}`,name:f.name,city:f.city,headline:f.useful||f.can_help||"Можливість",can_help:[f.can_help,f.have,f.useful].filter(Boolean).join(". "),can_share:f.can_share,needs:f.needs,contact:f.contact};const r=await supabase.from("profiles").insert(payload);if(r.error){setError(r.error.message);return}}setSaved(true)}return <main className="page"><section className="profileShell"><span className="kicker">ATLAS</span><h1>{t.profile}</h1><form className="profileForm" onSubmit={submit}>{fields.map(([n,l],i)=><label key={n}><span>{l}</span>{i<2||n==="contact"?<input required={n==="name"||n==="contact"} value={f[n]} onChange={e=>setF({...f,[n]:e.target.value})}/>:<textarea value={f[n]} onChange={e=>setF({...f,[n]:e.target.value})}/>}</label>)}{error&&<div className="error">{error}</div>}{saved&&<div className="success">{t.saved}</div>}<button className="primary">{t.save}</button></form></section></main>}
